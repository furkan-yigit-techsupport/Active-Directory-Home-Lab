# Active-Directory-Home-Lab
I set up a Windows Server 2022 AD environment using VMware, configured DNS and DHCP, domain-linked computers and a variety of users,  deployed Group Policies.

Snapshot 1 - Installed the server, ADDS, configured DNS and created DHCP scope for domain devices, assigned a static IP address to the Domain Controller.
